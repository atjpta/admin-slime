export enum StatKey {
  ATTACK = 'attack',
  MAGIC = 'magic',
  DEFENSE = 'defense',
  HP = 'hp',
}

export enum StatType {
  FLAT = 'flat',
  PERCENT = 'percent',
}

export enum StatSource {
  BASE = 'base',
  EQUIPMENT = 'equipment',
}
